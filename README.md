'
J'ai eu un souci avec le dossier "frontend"
Donc je vous prierai d'utiliser le contenu du dossier "front end"
au moment de vos compilations et tests.
C'est ce dernier qui contient le code mis à jour.

Décompressez simplement les archives contenus dans ce dossier,
décompressez le dossier backend là où il se trouve, avant de 
compiler et de procéder aux tests.
